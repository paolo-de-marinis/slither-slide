#ifndef COLLECTIBLE_RENDER_H
#define COLLECTIBLE_RENDER_H

#include <stdbool.h>

void collectibleRender(float worldX,
                       float worldY,
                       bool isCoin,
                       bool isFinalLevel,
                       float rotation);

#endif
